<IfModule mod_rewrite.c>
  RewriteEngine On

  # If you run AGMP under a subdirectory (e.g. /AGMP/public), keep the requested URL
  # and route everything to index.php.
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^ index.php [L]
</IfModule>
